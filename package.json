{
  "devDependencies": {
    "@babel/preset-env": "^7.18.6",
    "jest": "^28.1.1"
  },
  "scripts": {
    "test": "jest --env=jsdom --coverage"
  },
  "dependencies": {
    "jest-environment-jsdom": "^28.1.1"
  }
}
